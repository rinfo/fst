<?xml version="1.0" encoding="utf-8"?>
<rdf:RDF xmlns:rdf="http://www.w3.org/1999/02/22-rdf-syntax-ns#"
    xmlns:dct="http://purl.org/dc/terms/"
    xmlns:dces="http://purl.org/dc/elements/1.1/"
    xmlns:foaf="http://xmlns.com/foaf/0.1/"
    xmlns="http://rinfo.lagrummet.se/ns/2008/11/rinfo/publ#">

    <Myndighetsforeskrift rdf:about="{{foreskrift.get_rinfo_uri}}">
        <forfattningssamling rdf:resource="{{foreskrift.forfattningssamling.identifierare}}"/>
        <dct:title xml:lang="sv">{{foreskrift.titel}}</dct:title>
        <dct:identifier>{{foreskrift.identifierare}}</dct:identifier>
        <arsutgava>{{foreskrift.arsutgava}}</arsutgava>
        <lopnummer>{{foreskrift.lopnummer}}</lopnummer>

        <beslutsdatum rdf:datatype="http://www.w3.org/2001/XMLSchema#date">{{foreskrift.beslutsdatum|date}}</beslutsdatum>
        <ikrafttradandedatum rdf:datatype="http://www.w3.org/2001/XMLSchema#date">{{foreskrift.ikrafttradandedatum|date}}</ikrafttradandedatum>
        <utkomFranTryck rdf:datatype="http://www.w3.org/2001/XMLSchema#date">{{foreskrift.utkom_fran_tryck|date}}</utkomFranTryck>
        <dct:publisher rdf:resource="{{publisher_uri}}"/>
        {% if foreskrift.andrar %}
        <andrar rdf:resource="{{foreskrift.andrar.get_rinfo_uri}}"/>
        {% if foreskrift.omtryck %}
        <omtryckAv rdf:resource="{{foreskrift.andrar.get_rinfo_uri}}"/>{% endif %}
        {% endif %}
        {% if foreskrift.celexreferenser.all %}
        {% for referens in foreskrift.celexreferenser.all %}
        <genomforDirektiv rdf:resource="http://rinfo.lagrummet.se/ext/eur-lex/{{referens.celexnummer}}"/>{% endfor %}{% endif %}
        {% for bemyndigande in foreskrift.bemyndiganden.all %}
        <bemyndigande>
            <Forfattningsreferens>
                <angerGrundforfattning rdf:resource="http://rinfo.lagrummet.se/publ/sfs/{{bemyndigande.sfsnummer}}"/>
                {% if bemyndigande.kapitelnummer %}
                <angerKapitelnummer>{{bemyndigande.kapitelnummer}}</angerKapitelnummer>
                {% endif %}
                <angerParagrafnummer>{{bemyndigande.paragrafnummer}}</angerParagrafnummer>
            </Forfattningsreferens>
       </bemyndigande>{% endfor %}
        {% for amnesord in foreskrift.amnesord.all %}
        <dces:subject xml:lang="sv">{{amnesord.titel}}</dces:subject>{% endfor %}
        {% for bilaga in foreskrift.bilagor.all %}
            {% if bilaga.titel %}
                <bilaga>
                    <rdf:Description rdf:about="{{foreskrift.get_rinfo_uri}}#bilaga_{{forloop.counter}}">
                        <dct:title>{{bilaga.titel}}</dct:title>
                    </rdf:Description>
                </bilaga>
            {% endif %}
        {% endfor %}
    </Myndighetsforeskrift>

    {% for dok in foreskrift.ovriga_dokument.all %}
        <foaf:Document rdf:about="{{foreskrift.get_rinfo_uri}}/{{dok.file.url}}">
            <dct:title>{{dok.titel}}</dct:title>
            <foaf:primaryTopic rdf:resource="{{foreskrift.get_rinfo_uri}}"/>
        </foaf:Document>
    {% endfor %}

</rdf:RDF>
